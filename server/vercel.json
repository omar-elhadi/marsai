{
  "version": 2,
  "builds": [{ "src": "api/index.ts", "use": "@vercel/node" }],
  "routes": [{ "src": "/(.*)", "dest": "api/index.ts" }],
  "installCommand": "npm install && npx prisma generate --schema=prisma/schema.prisma"
}
