/**
 * index.js — LuminousButton barrel export
 *
 * Permet l'import depuis le dossier sans spécifier le fichier :
 *   import LuminousButton from '@/components/common/LuminousButton';
 *
 * Convention : tout composant dans components/common/ expose un index.js.
 */
export { default } from "./LuminousButton";
